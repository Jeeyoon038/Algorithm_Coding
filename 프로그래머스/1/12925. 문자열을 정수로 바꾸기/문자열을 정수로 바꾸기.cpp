#include <string>
#include <sstream>
#include <vector>

using namespace std;

int solution(string s) {
    int answer = 0;
    
    answer=stoi(s);
    //stoi는 문자열을 정수로 바로 변환해 준다.
    return answer;
}